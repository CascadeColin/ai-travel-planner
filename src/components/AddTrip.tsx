export default function AddTrip() {
    // adding or modifying a trip
    // use AI to help the user generate trip details
    // once user is satisfied, let them click an add trip button
    return (
        <h1>Add/Edit a Trip</h1>
    )
}